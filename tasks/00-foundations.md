# Task 00 — Foundations
Read CLAUDE.md and docs/SCOPE.html (Sections 3, 2) first.

Build: Next.js (App Router, TS) scaffold; Supabase client wiring (env vars); Supabase Auth
email/password login; role + site model per `app_users`; route guards so each role sees only
its permitted screens and site; app shell with the header/tab layout from docs/PROTOTYPE_REFERENCE.jsx
(logged-in user name + site shown top right). Seed one test user per role and put credentials in
the PR description. Deploy must succeed on Vercel.
Out: any feature screens. Empty tab stubs only.

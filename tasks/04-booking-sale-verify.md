# Task 04 — Booking, sale, admin verify queue
Booking modal (date/time/programme suggested from age via programmes min/max), re-book flow for
no-shows (rebook counter), sale flow (first class + payment tick hard requirement, Jotform warning),
didn't-enrol with reason → Lost or Nurture (future date required), undo. Admin verify queue for
pending sales (iClassPro checklist per SCOPE Section 7); only verified sales count.

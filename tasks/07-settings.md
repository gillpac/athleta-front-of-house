# Task 07 — Settings backend (management)
Blockout days per site (feeds op-day pace), programme management (create/rename/archive,
min/max age), checklist editor per role+site, user management (create users, set role/site,
deactivate). All audited.

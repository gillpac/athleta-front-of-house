# Task 01 — Data layer + seed
Apply supabase/schema.sql. Add RLS: receptionist/site_lead rows limited to own site; admin/management both.
No hard deletes — repository functions only archive. Audit-log writes on every mutation.
Seed the prototype's example records (docs/PROTOTYPE_REFERENCE.jsx seed block) incl. the Osman
two-child family, so previews always have reviewable data.
